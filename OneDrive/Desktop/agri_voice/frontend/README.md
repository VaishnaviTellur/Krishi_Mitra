# Frontend (Krishi Mitra)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Build for production:

```bash
cd frontend
npm run build
npm run preview
```

Notes:
- This project uses Vite + React + Tailwind CSS + Framer Motion.
- If you add or change Tailwind config, restart the dev server.
